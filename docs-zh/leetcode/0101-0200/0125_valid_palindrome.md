---
link: https://leetcode.cn/problems/valid-palindrome/
tags:
  - 简单
  - 双指针
  - 字符串
---
## 题目描述
如果在将所有大写字符转换为小写字符、并移除所有非字母数字字符之后，短语正着读和反着读都一样。则可以认为该短语是一个 **回文串** 。

字母和数字都属于字母数字字符。

给你一个字符串 `s`，如果它是 **回文串** ，返回 `true`；否则，返回`false`。

**示例 1：**

```
输入: s = "A man, a plan, a canal: Panama"
输出：true
解释："amanaplanacanalpanama" 是回文串。
```

**示例 2：**

```
输入：s = "race a car"
输出：false
解释："raceacar" 不是回文串。
```

**示例 3：**

```
输入：s = " "
输出：true
解释：在移除非字母数字字符之后，s 是一个空字符串 "" 。
由于空字符串正着反着读都一样，所以是回文串。
```

**提示：**

- `1 <= s.length <= 2 * 10^5`
- `s` 仅由可打印的 ASCII 字符组成

## 题目解析

### 解法1

**双指针法**：使用左右双指针从两端向中间移动，跳过非字母数字字符，逐一比较字符是否相等。

**原理：**
回文串的特性是正着读和反着读相同，因此可以使用双指针从字符串两端同时向中间逼近，每次比较两个指针指向的有效字符（字母或数字），若全部匹配则为回文串。

**步骤：**
1. 初始化左指针 `l` 指向字符串开头，右指针 `r` 指向字符串末尾
2. 当 `l < r` 时循环：
   - 左指针跳过所有非字母数字字符
   - 右指针跳过所有非字母数字字符
   - 若 `l >= r`，说明已完成比较，跳出循环
   - 将两个指针指向的字符转换为小写后比较，若不相等则返回 `false`
   - 左指针右移，右指针左移
3. 循环结束后返回 `true`

```embed-go
PATH: "vault://leetcode/0101-0200/0125_valid_palindrome/solution.go"
TITLE: "leetcode 125.验证回文串"
```
